<%= render 'genre_search', genres: @genres %>


<h2><%= @genre.name %>一覧</h2><span>全<%= @items.count %>件</span>
<% @items.each do |item| %>
  <%= image_tag item.get_image_id, size: '50×50' %>
  <%= item.name %>
  ￥<%= item.price %>
<% end %>
<%= paginate @items %>